# Database Configuration
# SECURITY: Never commit actual credentials to version control!
# Copy this file to .env and set your own secure values

# Database credentials (REQUIRED)
# Use strong, unique passwords in production
POSTGRES_USER=your_database_user
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=agrogame

# Optional: Override individual connection parameters
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432

# Or use a complete connection string instead
# DATABASE_URL=postgresql://user:password@host:port/database

# API Keys (REQUIRED for respective features)
# Get your DeepSeek API key from https://platform.deepseek.com/
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# Get your OpenWeather API key from https://openweathermap.org/api
OPENWEATHER_API_KEY=your_openweather_api_key_here

# Application Configuration
# APP_PORT=4000

# Default farm coordinates (optional)
# DEFAULT_FARM_LAT=55.7558
# DEFAULT_FARM_LON=37.6173

# IMPORTANT SECURITY NOTES:
# 1. Never commit the .env file to version control
# 2. Use strong, unique passwords (minimum 16 characters, mix of letters, numbers, symbols)
# 3. Rotate credentials regularly in production
# 4. Use different credentials for development, staging, and production
# 5. Ensure .env is listed in .gitignore
